import styled from "styled-components";

const MessageWrapper = styled.div`color: rgba(0, 0, 0, 0.87);`;

const Message = styled.p`
  font-size: 24px;
  font-weight: 400;
  color: red;
`;
export default {
  MessageWrapper,
  Message
};
